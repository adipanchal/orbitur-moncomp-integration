// Placeholder for later: form enhancements, client-side validation, ajax flows.
// Currently we use admin-post (non-AJAX) submissions so this file is optional.
jQuery(function($){
  // Example: focus first field on login page
  $('.orbitur-login-form input[type="email"]').first().focus();
});